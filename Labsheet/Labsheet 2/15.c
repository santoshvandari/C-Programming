//WAP to find if the given no. is positive or negative (use ternary operator).
#include<stdio.h>
void main()
{
	int a;
	printf("Enter the Number:- ");
	scanf("%d",&a);
	(a>0) ? 
	printf("%d is Positive Number.",a)

	: printf("%d is Negative Number.",a);
}
